<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:components="clr-namespace:Module.WarhammerTools.ViewModels.Components"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="Module.WarhammerTools.Views.Components.CharacterGroupView"
             x:DataType="components:CharacterGroupViewModel">
    <UserControl.Styles>
        <StyleInclude Source="avares://Module.WarhammerTools/Styles/CharacterSheetStyle.axaml"/>
    </UserControl.Styles>
    
    <Design.DataContext>
        <components:CharacterGroupViewModel/>    
    </Design.DataContext>
    
    <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto"
          ColumnDefinitions="Auto,*">
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                   Classes="title"
                   Text="GROUPE"/>

        <TextBlock Grid.Row="1" Grid.Column="0"
                   Classes="small"
                   HorizontalAlignment="Center"
                   xml:space="preserve">
            Nom du
            groupe
        </TextBlock>
        
        <TextBox Grid.Row="1" Grid.Column="1"
                 Text="{Binding Name}"/>

        <TextBlock Grid.Row="2" Grid.Column="0"
                   Classes="small"
                   HorizontalAlignment="Center"
                   xml:space="preserve">
            A court
            terme
        </TextBlock>
        
        <TextBox Grid.Row="2" Grid.Column="1"
                 Text="{Binding ShortTerm}"/>

        <TextBlock Grid.Row="3" Grid.Column="0"
                   Classes="small"
                   HorizontalAlignment="Center"
                   xml:space="preserve">
            A long
            terme
        </TextBlock>

        <TextBox Grid.Row="3" Grid.Column="1"
                 Text="{Binding LongTerm}"/>

        <TextBox Grid.Row="2" Grid.Column="1"
                 Text="{Binding ShortTerm}"/>

        <TextBlock Grid.Row="4" Grid.Column="0"
                   Classes="small"
                   HorizontalAlignment="Center">
            Membres
        </TextBlock>

        <TextBox Grid.Row="4" Grid.Column="1"
                 Text="{Binding Members}"/>

    </Grid>
    
</UserControl>
